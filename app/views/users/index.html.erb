<h1>Users</h1>

<div id="users-list">
  <% @users.try(:each) do |user| %>
	  <div id="users-list-item"> 
	  	<%= gravatar_for user, :size => 50 %> <%= link_to user.username, user_url(user) %>
	  </div>
  <% end %>
</div>