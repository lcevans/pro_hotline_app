<li class="question-mini">
	<div class="item side-item vote-side-item">
	  <div class="sub-item">
	    <%= question.voteCount() %>
	  </div>
	  <div class="sub-item">
	    <%= owl.pluralize("Vote", question.voteCount()) %>
	  </div>
	</div>
	<div class="item side-item answer-side-item" data-best="<%= (answered ? "true" : "") %>">
	  <div class="sub-item">
	    <%= question.answers.length %>
	  </div>
	  <div class="sub-item">
	    <%= owl.pluralize("Answer", question.answers.length) %>
	  </div>
	</div>
	<div class="item side-item view-side-item">
	  <div class="sub-item">
	    <%= question.viewCount %>
	  </div>
	  <div class="sub-item">
	    <%= owl.pluralize("View", question.viewCount) %>
	  </div>
	</div>
	<div class="item">
	<a href="/questions/<%= question.escape("id")%>" class="lead">
	  <%= question.escape("title") %> 
	</a>
	--
	<a href="/users/<%= question.author.id %>">
	  <%= question.author.username %>
	</a>
	<span>asked <%= question.createdAgo() %></span>
	<ul class="tags">
		<% question.tags.each(function (tag) { %>
		  <li class="tag label label-info">
		    <a href="/tags/<%= tag.id %>">
		      <%= tag.escape("name") %>
		    </a>
		  </li>
		<% }); %>
  </ul>
  <div>
</li>